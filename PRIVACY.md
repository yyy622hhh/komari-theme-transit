# Transit 隐私说明

本文说明 Transit 主题自身会在浏览器中发起哪些请求、哪些功能会产生访客数据，以及站点运营者在启用可选功能前应完成的检查。Komari 服务端、反向代理、CDN 和主机商的日志不由 Transit 控制，请分别查阅它们的配置与隐私条款。

## 默认关闭的访客功能

### 访客公网信息

主题配置 `visitorInfoEnabled` 默认为 `false`。关闭时不会加载访客公网 IP 卡片，也不会向下列公网信息服务发起该功能的请求。

开启后，访客浏览器会按顺序尝试：

1. `https://ipwho.is/`
2. `https://ipapi.co/json/`
3. `https://api.ip.sb/geoip`

查询在第一家成功返回后停止。被请求的服务会自然获得访客公网 IP，并可能依据自己的政策记录请求；返回的 IP、城市、地区、国家和网络组织信息仅用于当前浏览器界面。Transit 不会把这份返回值再上传到项目作者的服务器。

### Transit 访客审计

客户端配置 `visitorAuditClientEnabled` 默认为 `false`。只有它与 Komari 核心公开设置 `visitor_audit_enabled` **同时为 true** 时，Transit 才会向当前站点自己的 Komari 上报访客事件。

可能上报的字段包括：

- 页面路径、路由名、目标节点 ID 和 URL 查询参数的键名（不包含查询值）；
- 临时会话 ID、主题版本和色觉模式；
- 平台、浏览器品牌、语言、时区、屏幕与视口尺寸、像素比；
- CPU 逻辑核心数、设备内存、触控点、Cookie/DNT/自动化标志；
- 网络类型提示、WebGL 厂商与渲染器；
- WebRTC 候选类型、协议、地址种类，以及由候选签名生成的站点范围哈希；
- 由浏览器和设备特征生成的站点范围 SHA-256 指纹。

哈希指纹属于假名化标识，不等于匿名数据。主题会过滤名称包含 password、secret、token、cookie、clipboard、command 等敏感词的详情字段，并限制字段数量与长度，但运营者仍应把访客审计视为个人数据处理。

事件写入当前 Komari 的审计日志；访问权限、服务端记录的 IP/User-Agent、留存期限和删除流程由站点运营者与 Komari 配置决定。Transit 项目作者不会收到这些记录。

## 其他外部请求

即使上述两项保持关闭，当前版本仍可能产生以下外部请求：

- Iconify 图标：缺少本地缓存时，浏览器可能向 `api.iconify.design` 请求界面图标。
- 汇率：只有当前页面需要显示金额时才会读取本地日缓存；缓存缺失或过期时依次尝试 `open.er-api.com` 与 `api.frankfurter.app`。未登录隐藏价格或当前卡片不含金额时不会发起该请求。
- 节点地理信息：仅在已登录用户打开需要地理增强的工具时，可能把被查询的**服务器 IP**发送给 `api.ip.sb`、`ipinfo.io`、`ipapi.co` 或 `ipwho.is`；这些服务同时会看到浏览器出口 IP。
- 自定义背景：运营者配置外部图片或视频 URL 后，浏览器会直接请求相应主机。
- 外部链接：仅在访客主动点击后访问。

公开首页的地球节点按 Komari 已提供的国家/地区代码使用主题内置坐标聚合。该路径不读取节点 IPv4/IPv6，也不会为地图向第三方地理服务发送节点 IP。上文的节点地理信息查询仅适用于已登录用户主动打开、且通过权限校验的地理增强工具。

这些第三方服务由各自运营者控制，Transit 无法承诺其日志、留存或跨境处理方式。对严格内网或零第三方请求场景，部署者应通过内容安全策略、反向代理、本地资源替代和浏览器网络审计进一步限制。

## 运营者启用前检查清单

启用访客公网信息或访客审计前，建议至少完成：

1. 在站点隐私政策中说明用途、字段、接收方、法律依据、保留期限和联系方式。
2. 根据适用地区决定是否需要事前同意，并提供撤回或退出方式。
3. 限制 Komari 管理员和审计日志访问权限，设置合理的日志保留与删除策略。
4. 使用浏览器开发者工具确认实际请求与文档一致。
5. 不要把审计日志、未打码截图或导出文件公开提交到 Issue。

如发现未在本文列出的数据流，请通过 [SECURITY.md](SECURITY.md) 中的方式私下报告。
